# ====================== Agent 版本模型参数配置 ======================
# Agent 版本使用与基础模型相同的 API，但使用不同的提示词和参数

doubao_seed:
  # 继承基础 doubao_seed 模型配置，可在此覆盖特定参数
  # temperature 等参数如需与基础模型不同，可在此指定

internVL:
  # 继承基础 internVL 模型配置，可在此覆盖特定参数


